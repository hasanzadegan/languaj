angular.module('myApp').controller('spellController', function ($rootScope, $scope, $path,StorageService) {
    // $scope.letterList = $scope.current.viewerData.selectedLevelLexicalPhrase.itemJSONObj.letterList;




});
